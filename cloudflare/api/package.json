{
  "name": "proveedores-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:apply:local": "wrangler d1 migrations apply proveedores_db --local",
    "d1:apply:remote": "wrangler d1 migrations apply proveedores_db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
